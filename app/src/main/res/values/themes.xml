<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KitchwaApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Puedes personalizar tu tema aquí -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema para la pantalla de inicio -->
    <style name="Theme.APISplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.KitchwaApp</item>
    </style>

    <!-- Tema principal corregido para heredar correctamente de AppCompat -->
    <style name="Theme.Kitchwa_app" parent="Theme.KitchwaApp" />
</resources>
